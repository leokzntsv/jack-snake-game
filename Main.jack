class Main {
    function void main() {
        var SnakeGame game;
        var boolean debug;
        let debug = true;
        let game = SnakeGame.new(debug);
        do game.run();
        do game.dispose();
        return;
    }
}
