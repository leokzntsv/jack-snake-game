class Main {
    function void main() {
        var SnakeGame game;
        var boolean debug;

        do Random.setSeed(12345);

        let debug = false;
        let game = SnakeGame.new(debug);

        do game.run();
        do game.dispose();
        return;
    }
}
